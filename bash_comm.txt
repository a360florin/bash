git init
git clone <url>
git branch / git branch -a / git branch -r

git status
git add .
git commit -m "text"
git push origin <remote branch>

git checkout <branch name>
git branch -d <branch name>

git clone <url> 

git commit -m <directory to be added/>